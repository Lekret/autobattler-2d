using Services.StaticData;
using UnityEngine;
using Zenject;

namespace Ui
{
    public class SandboxConfiguration : MonoBehaviour
    {
        public GameObject ButtonRoot;

        [Inject] private ICharacterDataService _characterDataService;

        private void Start()
        {
            foreach (var )
        }
    }
}